import Landing from "@/components/mainLayouts/Landing";
import React from "react";

const LandingPageRoute = () => { // Renamed component to avoid conflict with 'page' filename convention if it causes issues
    return <Landing />;
};

export default LandingPageRoute;